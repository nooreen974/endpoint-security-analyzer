# endpoint-security-analyzer
Windows endpoint security analyzer for detecting security products, service issues, driver conflicts, firewall status, event log errors, and potential endpoint security conflicts.
